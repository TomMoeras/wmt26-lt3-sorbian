Final prediction files of the primary system as submitted to OCELoT (five files, official
schema). MANIFEST_sha256.json is the byte-verification target used by
scripts/reproduce_primary.sh. The bundle reflects the final per-test-set primary designations,
including the probe-day SC and MR improvements.
